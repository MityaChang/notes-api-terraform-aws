Add provider & version .tf in main module
2, Need tfvars
3. Add a build cache for docker => It will create a new ECR to cache , because docker file install the requirement 
4. Add Provider.tf add assumerole ARN => Taking from gitlab variable 
5. S3 bucket to store the state and log
6. Front end ec2 FE , public IP
Notes:

Infra use artifact , build use ECR
- Artifact will store in tfplan
- Gitlab can see the artifact , AWS will see the image in the ECR


Compute folder
- Anything that will use CPU resources
